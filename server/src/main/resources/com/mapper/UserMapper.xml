<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapper.UserMapper">

	<insert id="save">
		INSERT INTO user (email, username, department, nickname, rank_type, age)
		VALUES (#{email}, #{username}, #{department}, #{nickname}, #{rank}, #{age})
	</insert>

	<select id="findByEmail" resultType="User">
		SELECT * 
		  FROM user
		WHERE user.email = #{email}
	</select>
	
	<select id="findByEmailAndPassword" resultType="User">
		SELECT *
		  FROM user
		 WHERE user.email = #{email}
           AND user.password = #{password}
	</select>

	<update id="update">
		UPDATE review
		   SET title = #{title}
		     , writer = #{writer}
		     , content = #{content}
		 WHERE board_no = #{boardNo}
	</update>

	<delete id="delete">
		DELETE FROM review WHERE board_no = #{boardNo}
	</delete>
	
</mapper>